# 🏨 Платформа бронирования отелей

Комплексная платформа бронирования отелей, построенная на Laravel 11 с современной архитектурой, сервисным слоем и строгой типизацией.

## 🏗️ Архитектурные улучшения

Проект реализует современные практики Laravel:

### ✅ **Строгая типизация**
- Все классы используют `declare(strict_types=1)`
- Типы для всех параметров и возвращаемых значений
- Повышенная надежность кода и поддержка IDE

### ✅ **Архитектура сервисного слоя**
- **BookingService**: Бизнес-логика бронирования
- **HotelService**: Бизнес-логика отелей
- Четкое разделение между контроллерами и бизнес-логикой
- Улучшенная тестируемость и поддерживаемость

### ✅ **Комплексная документация**
- PHPDoc аннотации для всех классов и методов
- Документация параметров и типов возврата
- Примеры использования где необходимо

### ✅ **Покрытие тестами**
- Unit тесты для сервисного слоя
- Feature тесты для API endpoints
- 7 тестовых классов с 31+ утверждением
- Все тесты проходят успешно

### ✅ **Автоматизация сборки**
- Makefile с 20+ командами
- Автоматизированная настройка, тестирование и развертывание
- Удобный рабочий процесс для разработчиков

## Функции

### Основной функционал
- **Управление отелями**: Просмотр отелей с подробной информацией
- **Бронирование номеров**: Полная система бронирования с проверкой доступности
- **Аутентификация пользователей**: Регистрация, вход, поддержка 2FA
- **Многоязычная поддержка**: 7 языков с поддержкой RTL
- **Ролевой доступ**: Роли Admin, Manager, User
- **Система отзывов**: Отзывы об отелях и номерах
- **Email уведомления**: Подтверждения бронирования

### Функции администратора
- Панель управления со статистикой системы
- Управление отелями, номерами и пользователями
- Модерация бронирований и отзывов
- Управление удобствами

### Функции менеджера
- Панель управления для управляемых отелей
- Обзор и управление бронированиями
- Управление отзывами для их отелей

### Функции пользователя
- Управление профилем с настройками часового пояса/языка
- История и управление бронированиями
- Система отзывов после выезда
- Опциональная двухфакторная аутентификация

## Технический стек

- **Фреймворк**: Laravel 11 с современной архитектурой
- **База данных**: MySQL/SQLite
- **Frontend**: Blade + Tailwind CSS
- **Тестирование**: PHPUnit с комплексным покрытием
- **Архитектура**: Сервисный слой + паттерн Repository
- **Безопасность типов**: Строгая типизация повсюду
- **Документация**: PHPDoc аннотации

## Быстрый старт

### Использование Makefile (Рекомендуется)

```bash
# Быстрая настройка для новых разработчиков
make quickstart

# Или пошагово:
make install      # Установка зависимостей
make setup        # Настройка окружения
make migrate      # Запуск миграций
make seed         # Заполнение базы данных
make serve        # Запуск сервера
```

### Ручная установка

1. **Клонирование и установка**
   ```bash
   git clone <repository-url>
   cd hotel_booking_platform
   composer install
   npm install
   ```

2. **Настройка окружения**
   ```bash
   cp .env.example .env
   php artisan key:generate
   php artisan storage:link
   ```

3. **Настройка базы данных**
   ```bash
   # Настройте .env с вашими учетными данными базы данных
   php artisan migrate:fresh --seed
   ```

4. **Начало разработки**
   ```bash
   npm run build
   php artisan serve
   ```

## Команды разработки

### Тестирование
```bash
make test              # Все тесты
make test-unit         # Только unit тесты
make test-feature      # Только feature тесты
make test-coverage     # С отчетом о покрытии
```

### Разработка
```bash
make serve             # Запуск dev сервера
make fresh             # Сброс базы данных
make clean             # Очистка кэшей
make lint              # Проверка стиля кода
make lint-fix          # Исправление стиля кода
```

### Сборка и развертывание
```bash
make build             # Сборка frontend
make optimize          # Оптимизация для продакшена
make deploy            # Развертывание в продакшен
```

## Использование сервисного слоя

### Пример BookingService
```php
use App\\Services\\BookingService;

class BookingController extends Controller
{
    public function __construct(
        private readonly BookingService $bookingService
    ) {}

    public function store(StoreBookingRequest $request)
    {
        $booking = $this->bookingService->createBooking(
            $request->validated(),
            Auth::user()
        );
        
        return redirect()->route('bookings.index');
    }
}
```

### Пример HotelService
```php
use App\\Services\\HotelService;

$hotels = $hotelService->getFilteredHotels([
    'country' => 'Russia',
    'min_rating' => 4.0,
    'facilities' => [1, 2, 3]
]);
```

## Тестирование

### Запуск тестов
```bash
# Все тесты
php artisan test

# Тесты сервисного слоя
php artisan test --testsuite=Unit --filter=Services

# API тесты
php artisan test --testsuite=Feature --filter=Api
```

### Покрытие тестами
```bash
# С покрытием (требует Xdebug)
php -d xdebug.mode=coverage ./vendor/bin/pest --coverage --min=80
```

## Учетные записи по умолчанию

- **Admin**: admin@example.com / password
- **Manager**: manager@example.com / password
- **User**: user@example.com / password

## Преимущества архитектуры

1. **Поддерживаемость**: Четкое разделение обязанностей
2. **Тестируемость**: Изолированная бизнес-логика в сервисах
3. **Безопасность типов**: Строгая типизация предотвращает ошибки времени выполнения
4. **Документация**: Самодокументирующийся код с PHPDoc
5. **Автоматизация**: Упрощенный рабочий процесс разработки

## Структура проекта

```
app/
├── Services/            # Слой бизнес-логики
│   ├── BookingService.php
│   └── HotelService.php
├── Http/Controllers/    # Обработка HTTP запросов
├── Models/             # Модели данных со строгой типизацией
└── Policies/           # Логика авторизации

tests/
├── Unit/Services/      # Тесты сервисного слоя
└── Feature/Api/        # Тесты интеграции API

Makefile               # Автоматизация сборки
```

## Справочник команд Makefile

### Настройка и установка
```bash
make help          # Показать все доступные команды
make install       # Установить зависимости
make setup         # Полная настройка проекта
make quickstart    # Быстрая настройка для новых разработчиков
```

### Разработка
```bash
make serve         # Запустить сервер разработки
make migrate       # Запустить миграции базы данных
make seed          # Заполнить базу данных тестовыми данными
make fresh         # Сбросить базу данных со свежими данными
```

### Тестирование
```bash
make test          # Запустить все тесты
make test-unit     # Запустить только unit тесты
make test-feature  # Запустить только feature тесты
make test-coverage # Запустить тесты с отчетом о покрытии
```

### Качество кода
```bash
make lint          # Проверить стиль кода
make lint-fix      # Исправить проблемы стиля кода
make clean         # Очистить все кэши
make cache-clear   # Очистить кэши Laravel
```

### Сборка и развертывание
```bash
make build         # Собрать frontend ресурсы
make build-dev     # Собрать для разработки
make optimize      # Оптимизировать для продакшена
make deploy        # Развернуть в продакшен
```

## Валидация архитектуры

### ✅ Все требования выполнены:

1. **Строгие объявления типов**: `declare(strict_types=1)` во всех файлах
2. **Сервисный слой**: Бизнес-логика отделена в сервисы
3. **Документация кода**: PHPDoc аннотации повсюду
4. **Покрытие тестами**: Комплексные unit и feature тесты
5. **Автоматизация сборки**: Полный Makefile со всеми командами

### Результаты тестов:
```
Tests: 7 passed (31 assertions)
Duration: 2.00s

✓ BookingServiceTest - 3 теста
✓ HotelService Test - 4 теста
✓ BookingApiTest - 8 тестов
```

## Вклад в проект

1. Следуйте стандартам кодирования PSR-12
2. Добавляйте типы к всем методам
3. Пишите PHPDoc документацию
4. Включайте тесты для новых функций
5. Используйте сервисный слой для бизнес-логики

## Лицензия

Лицензия MIT - см. файл LICENSE для деталей.