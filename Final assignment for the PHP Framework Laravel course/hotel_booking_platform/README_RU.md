# 🏨 Платформа Бронирования Отелей

Комплексная корпоративная платформа бронирования отелей, построенная на Laravel 12, с полной поддержкой многоязычности, продвинутым управлением бронированиями, общением в реальном времени и современным UI/UX дизайном.

## 🚀 Полная Установка с Нуля

### Требования
- PHP 8.2+
- MySQL 8.0+
- Composer
- Node.js & npm

### Шаг 1: Создание Базы Данных
```sql
-- Подключитесь к MySQL и создайте базу данных
CREATE DATABASE hotel_booking_platform;
```

### Шаг 2: Импорт Структуры Базы Данных
```bash
# Импортируйте полную схему базы данных со всеми таблицами и базовыми данными
mysql -u root -p hotel_booking_platform < database.sql
```

### Шаг 3: Установка Зависимостей
```bash
# Установка PHP зависимостей
composer install

# Установка Node.js зависимостей
npm install
```

### Шаг 4: Настройка Окружения
```bash
# Копирование файла окружения
cp .env.example .env

# Генерация ключа приложения
php artisan key:generate

# Настройте подключение к базе данных в файле .env:
# DB_CONNECTION=mysql
# DB_HOST=127.0.0.1
# DB_PORT=3306
# DB_DATABASE=hotel_booking_platform
# DB_USERNAME=root
# DB_PASSWORD=ваш_пароль
```

### Шаг 5: Заполнение Базы Данных Тестовыми Данными
```bash
# Заполните базу данных только отелями, номерами и удобствами
php artisan app:quick-setup
```

### Шаг 6: Запуск Приложения
```bash
# Запуск сервера разработки Laravel
php artisan serve

# В отдельном терминале, компиляция ресурсов
npm run build
```

### Шаг 7: Доступ к Приложению
- **Основной Сайт**: http://localhost:8000
- **Панель Администратора**: http://localhost:8000/admin (зарегистрируйте первого пользователя, затем повысьте до админа)
- **Панель Менеджера**: http://localhost:8000/manager (создайте аккаунт менеджера через админ-панель)

## 🚀 Быстрая Установка (Альтернативный Метод)

### Установка Базы Данных Одной Командой (если база данных уже существует)
```bash
# Полная настройка базы данных только с отелями, номерами и удобствами
php artisan app:quick-setup

# Альтернатива: Ручная настройка с опциями
php artisan app:setup-database --fresh
```

### Запуск Сервера Разработки
```bash
# Запуск сервера Laravel
php artisan serve

```

## 🌟 Ключевые Особенности

- **Поддержка Многоязычности**: 7 языков (Английский, Русский, Арабский, Французский, Немецкий, Итальянский, Испанский)
- **Ролевой Доступ**: Роли Администратора, Менеджера и Пользователя с определенными правами
- **Система Бронирования в Реальном Времени**: Проверка доступности в реальном времени с предотвращением конфликтов
- **Система Отзывов и Рейтингов**: Отзывы гостей с модерацией и процессом одобрения
- **Двухфакторная Аутентификация**: Повышенная безопасность с поддержкой 2FA
- **Адаптивный Дизайн**: Mobile-first дизайн, оптимизированный для всех устройств
- **Email Уведомления**: Автоматические подтверждения бронирований и напоминания
- **RESTful API**: Полный JSON API для внешних интеграций и тестирования

## 👥 Регистрация Пользователей

После выполнения `php artisan app:quick-setup`:

### По Умолчанию Пользователи Не Создаются
- **Таблица Пользователей**: Полностью пустая по замыслу
- **Регистрация**: Пользователи должны регистрироваться через интерфейс приложения
- **Первый Пользователь**: Первого зарегистрированного пользователя можно повысить до админа вручную в базе данных
- **Доступ Админа**: Обновите поле `role` на `admin` в таблице users для административного доступа

### Доступные Роли Пользователей
- **admin**: Полное системное администрирование, управление пользователями, управление отелями
- **manager**: Управление конкретным отелем, надзор за бронированиями назначенных отелей
- **user**: Создание бронирований, управление профилем, отзывы (роль по умолчанию)

## 🌍 Поддержка Многоязычности

### Поддерживаемые Языки
- **Английский** (en) - USD ($)
- **Русский** (ru) - RUB (₽)
- **Арабский** (ar) - AED (د.إ) с поддержкой RTL
- **Французский** (fr) - EUR (€)
- **Немецкий** (de) - EUR (€)
- **Итальянский** (it) - EUR (€)
- **Испанский** (es) - EUR (€)

### Особенности
- Полный перевод UI для всех языков
- Автоматическая конвертация валют на основе локали
- Поддержка письма справа налево (RTL) для арабского языка
- Управление часовыми поясами и форматирование дат
- Локализованные email уведомления

## 🌐 Поддержка Часовых Поясов

### Глобальная Совместимость
- **Хранение в UTC**: Все временные метки хранятся в UTC в базе данных
- **Пользовательские Часовые Пояса**: Каждый пользователь может установить свой часовой пояс
- **Автоматическая Конвертация**: Время автоматически конвертируется в локальный часовой пояс пользователя
- **Поддержка Всех Часовых Поясов**: Поддерживает любые валидные PHP часовые пояса
- **Точное Местное Время**: Показывает точное местное время в любой стране мира

## 🛠️ Технологический Стек

- **Фреймворк**: Laravel 12 (Последняя LTS)
- **PHP**: 8.2+ с современными возможностями
- **База Данных**: MySQL 8.0+ с оптимизированными запросами
- **Frontend**: Blade Templates + Tailwind CSS + Alpine.js
- **API**: RESTful JSON API
- **Аутентификация**: Laravel Breeze с расширениями 2FA
- **Тестирование**: PHPUnit с комплексным покрытием тестами
- **Кеширование**: Redis/Файловая система кеширования
- **Email**: Laravel Mail с поддержкой очередей

## 📊 Статистика Системы

После выполнения `php artisan app:quick-setup`, ваша база данных будет содержать:
- **200 Отелей** с подробными описаниями и удобствами
- **~1000 Номеров** с различными типами и ценами
- **24 Удобства** для отелей и номеров
- **0 Пользователей** (таблица пустая по замыслу)
- **0 Бронирований** (создаются пользователями)
- **0 Отзывов** (создаются пользователями)

## 🔒 Функции Безопасности

### Общая Безопасность
- **Контроль Доступа на Основе Ролей** (Администратор, Менеджер, Пользователь)
- **Двухфакторная Аутентификация** (Email/SMS)
- **CSRF Защита** на всех веб-формах
- **Предотвращение SQL Инъекций** с Eloquent ORM
- **XSS Защита** с экранированием Blade шаблонов
- **Хеширование Паролей** с bcrypt
- **Верификация Email** для новых аккаунтов
- **Ограничение Скорости** на API эндпоинтах

## 📈 Оптимизация Производительности

- **Индексирование Базы Данных** для быстрых запросов
- **Eager Loading** для предотвращения N+1 запросов
- **Стратегия Кеширования** для часто запрашиваемых данных
- **Оптимизация Ресурсов** с системой сборки Vite
- **Оптимизация Изображений** для фотографий отелей/номеров
- **Оптимизация Запросов** с мониторингом базы данных
- **Кеширование API Ответов** для публичных эндпоинтов

## 🐛 Устранение Неполадок

### API Возвращает HTML Вместо JSON
**Проблема**: Получение HTML ответа от `/hotels` вместо JSON  
**Решение**: Используйте эндпоинт `/api/hotels` для JSON ответа

### Ошибка 419 (CSRF Token Mismatch)
**Проблема**: CSRF ошибка при тестировании API  
**Решение**: 
- Используйте маршруты `/api/*` (исключены из CSRF)
- Убедитесь, что `APP_ENV=local` в `.env`
- Очистите кеш: `php artisan config:clear`

### Ошибка 404 на API Маршрутах
**Проблема**: API маршруты не найдены  
**Решение**:
- Проверьте, что `api.php` зарегистрирован в `bootstrap/app.php`
- Очистите кеш маршрутов: `php artisan route:clear`
- Проверьте маршруты: `php artisan route:list --path=api`

## 🧪 Тестирование

### Запуск Тестов
```bash
# Запуск всех тестов
php artisan test

# Запуск конкретного теста
php artisan test --filter DatabaseStructureTest

# Запуск тестов с покрытием
php artisan test --coverage
```

### Типы Тестов
- **Unit Tests**: Тестирование отдельных компонентов
- **Feature Tests**: Тестирование функциональности приложения
- **API Tests**: Тестирование API эндпоинтов
- **Performance Tests**: Тестирование производительности
- **Security Tests**: Тестирование безопасности

## 📄 Заявление об Образовательных Целях

Эта **Платформа Бронирования Отелей** была создана как комплексный образовательный проект для демонстрации продвинутых навыков веб-разработки и современных практик программной инженерии.

### Продемонстрированные Технические Навыки
- **Full-Stack Разработка**: Полное веб-приложение от базы данных до пользовательского интерфейса
- **Дизайн RESTful API**: Чистый, хорошо документированный JSON API
- **Экспертиза Laravel**: Продвинутые возможности Laravel, лучшие практики и инструменты экосистемы
- **Многоязычные Приложения**: Интернационализация, локализация и культурная адаптация
- **Дизайн Базы Данных**: Сложные отношения, оптимизация и целостность данных
- **Реализация Безопасности**: Аутентификация, авторизация и защита данных
- **Методологии Тестирования**: Unit тестирование, интеграционное тестирование и разработка через тестирование
- **Оптимизация Производительности**: Стратегии кеширования, оптимизация запросов и масштабируемость
- **Современный Frontend**: Адаптивный дизайн, прогрессивное улучшение и пользовательский опыт
- **Документация API**: Полная, ясная документация для разработчиков

### Демонстрация Профессионального Развития
- **Качество Кода**: Принципы чистого кода, паттерны проектирования SOLID и поддерживаемая архитектура
- **Документация**: Комплексная документация, комментарии к коду и техническое письмо
- **Управление Проектами**: Планирование функций, контроль версий и управление релизами
- **Решение Проблем**: Реализация сложной бизнес-логики и обработка крайних случаев
- **Отраслевые Стандарты**: Следование установленным конвенциям и лучшим практикам

### Не для Коммерческого Использования
Этот проект специально создан для образовательных целей и портфолио. Он демонстрирует технические возможности и служит учебным ресурсом для концепций веб-разработки, но не предназначен для коммерческого развертывания или производственного использования.

---

**Статус Проекта**: Образовательная Демонстрация  
**Цель**: Демонстрация Навыков и Учебный Ресурс  
**Поддержка**: Только Документация и Примеры

*Этот проект представляет комплексный пример современной разработки веб-приложений с использованием Laravel и демонстрирует способность разработчика создавать сложные, многофункциональные приложения, следуя лучшим отраслевым практикам.*

## 🛣️ Полная Документация API

### Базовый URL
```
http://localhost:8000/api
```

### 🔐 API Аутентификации (Только для Локальной Разработки)

**⚠️ ВАЖНО**: Эти эндпоинты доступны **ТОЛЬКО в локальной среде разработки** (`APP_ENV=local`). Они автоматически отключаются в продакшене.

#### 1. Регистрация Пользователя БЕЗ 2FA
```http
POST /api/register
```
**Среда**: Только локальная разработка  
**Content-Type**: `application/json`
**Заголовки**: `Accept: application/json`

**Тело Запроса**:
```json
{
    "full_name": "Иван Петров",
    "email": "ivan.petrov@example.com",
    "phone": "+79123456789",
    "date_of_birth": "1990-01-01",
    "gender": "male",
    "country": "Russia",
    "city": "Moscow",
    "address": "Красная площадь 1",
    "postal_code": "101000",
    "passport_number": "1234567890",
    "password": "password123",
    "password_confirmation": "password123",
    "language": "ru",
    "two_factor_enabled": 0
}
```

**Ответ (Успех 201)**:
```json
{
    "success": true,
    "message": "Регистрация успешна",
    "user": {
        "id": 1,
        "full_name": "Иван Петров",
        "email": "ivan.petrov@example.com",
        "role": "user"
    },
    "redirect": "/dashboard"
}
```

#### 2. Вход Пользователя БЕЗ 2FA
```http
POST /api/login
```
**Заголовки**: `Accept: application/json`
**Content-Type**: `application/json`

**Тело Запроса**:
```json
{
    "email": "ivan.petrov@example.com",
    "password": "password123",
    "remember": true
}
```

**Ответ (Успех 200)**:
```json
{
    "success": true,
    "message": "Вход выполнен успешно",
    "user": {
        "id": 1,
        "full_name": "Иван Петров",
        "email": "ivan.petrov@example.com",
        "role": "user"
    },
    "redirect": "/hotels"
}
```

#### 3. Регистрация Пользователя С 2FA
```http
POST /api/register
```
**Заголовки**: `Accept: application/json`
**Content-Type**: `application/json`

**Тело Запроса**:
```json
{
    "full_name": "Анна Смирнова",
    "email": "anna.smirnova@example.com",
    "phone": "+79123456790",
    "date_of_birth": "1990-01-01",
    "gender": "female",
    "country": "Russia",
    "city": "Moscow",
    "address": "Тверская 10",
    "postal_code": "101000",
    "passport_number": "0987654321",
    "password": "password123",
    "password_confirmation": "password123",
    "language": "ru",
    "two_factor_enabled": 1
}
```

**Ответ (Успех 201)**:
```json
{
    "success": true,
    "message": "Регистрация успешна",
    "user": {
        "id": 2,
        "full_name": "Анна Смирнова",
        "email": "anna.smirnova@example.com",
        "role": "user"
    },
    "redirect": "/two-factor/method"
}
```

#### 4A. Выбор Метода 2FA - Email
```http
POST /api/two-factor/method
```
**Заголовки**: `Accept: application/json`
**Content-Type**: `application/x-www-form-urlencoded`

**Тело (form-data)**:
```
method=email
```

**Ответ (Успех 200)**:
```json
{
    "success": true,
    "message": "Метод 2FA через email выбран",
    "redirect": "/api/two-factor/verify"
}
```

#### 4B. Выбор Метода 2FA - Google Authenticator
```http
POST /api/two-factor/method
```
**Заголовки**: `Accept: application/json`
**Content-Type**: `application/x-www-form-urlencoded`

**Тело (form-data)**:
```
method=google_authenticator
```

**Ответ (Успех 200)**:
```json
{
    "success": true,
    "message": "Требуется настройка Google Authenticator",
    "setup_data": {
        "manual_entry": {
            "account_name": "anna.smirnova@example.com",
            "secret_key": "GENERATED_SECRET_KEY",
            "issuer": "Hotel Booking Platform"
        }
    },
    "redirect": "/api/two-factor/verify"
}
```

#### 5. Проверка 2FA - Код из Email
```http
POST /api/two-factor/verify
```
**Заголовки**: `Accept: application/json`
**Content-Type**: `application/x-www-form-urlencoded`

**Тело (form-data)**:
```
code=123456
```
*Получите код из `storage/logs/laravel.log`*

**Ответ (Успех 200)**:
```json
{
    "success": true,
    "message": "Двухфакторная аутентификация завершена",
    "user": {
        "id": 2,
        "full_name": "Анна Смирнова",
        "email": "anna.smirnova@example.com",
        "role": "user"
    },
    "redirect": "/api/hotels"
}
```

#### 6. Проверка 2FA - Код из Google Authenticator
```http
POST /api/two-factor/verify
```
**Заголовки**: `Accept: application/json`
**Content-Type**: `application/x-www-form-urlencoded`

**Тело (form-data)**:
```
code=654321
```
*Получите код из приложения Google Authenticator*

**Ответ**: Такой же как при проверке Email 2FA

#### 7. Вход Пользователя С 2FA
```http
POST /api/login
```
**Заголовки**: `Accept: application/json`
**Content-Type**: `application/json`

**Тело Запроса**:
```json
{
    "email": "anna.smirnova@example.com",
    "password": "password123"
}
```

**Ответ (Успех 200)**:
```json
{
    "success": true,
    "message": "Требуется двухфакторная аутентификация",
    "two_factor_required": true,
    "redirect": "/api/two-factor/verify"
}
```

*Затем используйте шаг 5 или 6 для завершения проверки 2FA*

**Поддерживаемые Языки**:
- `"en"` - Английский
- `"ru"` - Русский  
- `"de"` - Немецкий
- `"fr"` - Французский
- `"it"` - Итальянский
- `"es"` - Испанский
- `"ar"` - Арабский

#### Выход Пользователя
```http
POST /api/logout
```
**Заголовки**: `Accept: application/json`

**Ответ (Успех 200)**:
```json
{
    "success": true,
    "message": "Выход выполнен успешно"
}
```

### 🏨 API Отелей (Публичный Доступ)

#### 8. Получить Все Отели (Аутентификация Не Требуется)

```http
GET /api/hotels
```
**Аутентификация**: Не требуется  
**Заголовки**: `Accept: application/json`

**Основной Запрос**:
```http
GET /api/hotels
```

**Ответ (Успех 200)**:
```json
{
    "success": true,
    "data": [
        {
            "id": 1,
            "title": "The Ritz-Carlton Moscow",
            "description": "Роскошный отель в самом сердце Москвы.",
            "address": "Tverskaya Street 3, Moscow",
            "country": "Russia",
            "city": "Moscow",
            "rating": 4.8,
            "manager_id": 2,
            "facilities": [
                {
                    "id": 1,
                    "title": "Wi-Fi",
                    "icon": "wifi"
                }
            ]
        }
    ],
    "pagination": {
        "current_page": 1,
        "last_page": 10,
        "per_page": 15,
        "total": 150
    }
}
```

#### 9. Фильтр Отелей по Стране
```http
GET /api/hotels?country=Russia
```
**Заголовки**: `Accept: application/json`

#### 10. Фильтр Отелей по Городу
```http
GET /api/hotels?city=Moscow
```
**Заголовки**: `Accept: application/json`

#### 11. Поиск Отелей по Тексту
```http
GET /api/hotels?search=роскошный
```
**Заголовки**: `Accept: application/json`

#### 12. Фильтр Отелей по Рейтингу
```http
GET /api/hotels?rating=4
```
**Заголовки**: `Accept: application/json`

#### 13. Фильтр Отелей по Ценовому Диапазону
```http
GET /api/hotels?price_from=5000&price_to=15000
```
**Заголовки**: `Accept: application/json`

#### 14. Фильтр Отелей по Удобствам
```http
GET /api/hotels?facilities[]=1&facilities[]=3
```
**Заголовки**: `Accept: application/json`
*Фильтр по Wi-Fi (ID: 1) и Бассейн (ID: 3)*

#### 15. Сортировка Отелей
```http
GET /api/hotels?sort=price_asc
```
**Заголовки**: `Accept: application/json`
**Опции Сортировки**: `price_asc`, `price_desc`, `rating_desc`, `name_asc`, `default`

#### 16. Пагинация Отелей
```http
GET /api/hotels?per_page=10&page=2
```
**Заголовки**: `Accept: application/json`

#### 17. Комбинированные Фильтры
```http
GET /api/hotels?country=Russia&rating=4&sort=price_desc&per_page=5
```
**Заголовки**: `Accept: application/json`

#### 18. Получить Доступные Страны
```http
GET /api/countries
```
**Заголовки**: `Accept: application/json`

#### 19. Получить Все Удобства
```http
GET /api/facilities
```
**Заголовки**: `Accept: application/json`

#### 20. Получить Детали Отеля
```http
GET /api/hotels/1
```
**Заголовки**: `Accept: application/json`

### 🛏️ API Доступности Номеров

#### 21. Проверить Доступность Номера
```http
POST /api/rooms/check-availability
```
**Аутентификация**: Не требуется  
**Заголовки**: `Accept: application/json`, `Content-Type: application/json`

**Тело Запроса**:
```json
{
    "room_id": 1,
    "started_at": "2025-12-01",
    "finished_at": "2025-12-05"
}
```

**Ответ (Доступен)**:
```json
{
    "available": true,
    "days": 4,
    "price_per_night": 150.00,
    "total_price": 600.00
}
```

### 👤 API Аутентифицированного Пользователя

**⚠️ Требуется Аутентификация**: Все эндпоинты ниже требуют входа в систему

#### 22. Получить Профиль Пользователя
```http
GET /api/profile
```
**Аутентификация**: Требуется (на основе сессии)  
**Заголовки**: `Accept: application/json`

#### 23. Создать Бронирование
```http
POST /api/bookings
```
**Аутентификация**: Требуется (на основе сессии)  
**Заголовки**: `Accept: application/json`, `Content-Type: application/json`

**Тело Запроса**:
```json
{
    "room_id": 1,
    "started_at": "2025-12-01",
    "finished_at": "2025-12-05",
    "special_requests": "Поздний заезд, пожалуйста"
}
```

#### 24. Получить Бронирования Пользователя
```http
GET /api/bookings
```
**Аутентификация**: Требуется (на основе сессии)  
**Заголовки**: `Accept: application/json`

#### 25. Обновить Бронирование
```http
PUT /api/bookings/1
```
**Аутентификация**: Требуется (на основе сессии)  
**Заголовки**: `Accept: application/json`, `Content-Type: application/json`

#### 26. Отменить Бронирование
```http
DELETE /api/bookings/1
```
**Аутентификация**: Требуется (на основе сессии)  
**Заголовки**: `Accept: application/json`

**Примечание**: Статус бронирования изменяется на "отменено", но остается в базе данных для аудита.

#### Получить Бронирования Пользователя
```http
GET /api/bookings
```
**Описание**: Получить бронирования аутентифицированного пользователя  
**Аутентификация**: Требуется (на основе сессии)

#### Создать Бронирование
```http
POST /api/bookings
```
**Описание**: Создать новое бронирование  
**Аутентификация**: Требуется (на основе сессии)  
**Content-Type**: `application/json`

**Тело Запроса**:
```json
{
    "room_id": 1,
    "started_at": "2025-12-01",
    "finished_at": "2025-12-05",
    "special_requests": "Поздний заезд, пожалуйста"
}
```

#### Обновить Бронирование
```http
PUT /api/bookings/{booking_id}
```
**Описание**: Обновить даты бронирования и особые пожелания  
**Аутентификация**: Требуется (на основе сессии)

#### Отменить Бронирование
```http
DELETE /api/bookings/{booking_id}
```
**Описание**: Отменить бронирование (изменяет статус на "cancelled")  
**Аутентификация**: Требуется (на основе сессии)

**Важные Примечания**:
- Бронирования НЕ удаляются из базы данных - статус меняется на "cancelled"
- Отмененные бронирования по-прежнему отображаются в `GET /api/bookings` со статусом "cancelled"
- Это сохраняет историю бронирований для аудита

### 👑 API Администратора

**⚠️ Требуется Роль Админа**: Все эндпоинты ниже требуют роль 'admin'

#### Как Стать Админом
1. Зарегистрируйте пользователя через API (шаги 1-2 выше)
2. Обновите роль пользователя в базе данных:
```sql
UPDATE users SET role = 'admin' WHERE email = 'your-email@example.com';
```
3. Войдите снова для получения прав админа

#### 27. Получить Всех Пользователей
```http
GET /api/admin/users
```
**Аутентификация**: Требуется (роль admin)  
**Заголовки**: `Accept: application/json`

#### 28. Получить Все Бронирования (Админ)
```http
GET /api/admin/bookings
```
**Аутентификация**: Требуется (роль admin)  
**Заголовки**: `Accept: application/json`

#### 29. Создать Отель
```http
POST /api/admin/hotels
```
**Аутентификация**: Требуется (роль admin)  
**Заголовки**: `Accept: application/json`, `Content-Type: application/json`

#### 30-35. Остальные Админские Операции
- `PUT /api/admin/hotels/{id}` - Обновить отель
- `DELETE /api/admin/hotels/{id}` - Удалить отель
- `POST /api/admin/rooms` - Создать номер
- `PUT /api/admin/rooms/{id}` - Обновить номер
- `DELETE /api/admin/rooms/{id}` - Удалить номер
- `GET /api/admin/stats` - Статистика системы

#### Получить Все Бронирования (Админ)
```http
GET /api/admin/bookings
```
**Описание**: Получить все бронирования в системе  
**Аутентификация**: Требуется (роль admin)

**Параметры Запроса**:
- `status` - Фильтр по статусу (confirmed, cancelled, pending)
- `hotel_id` - Фильтр по ID отеля
- `user_id` - Фильтр по ID пользователя

#### Создать Отель
```http
POST /api/admin/hotels
```
**Описание**: Создать новый отель  
**Аутентификация**: Требуется (роль admin)

**Тело Запроса**:
```json
{
    "title": "Новый Отель",
    "description": "Описание отеля",
    "address": "Адрес отеля",
    "country": "Russia",
    "city": "Moscow",
    "rating": 4.5,
    "manager_id": null,
    "poster_url": "https://images.unsplash.com/photo-1566073771259-6a8506099945?ixlib=rb-4.0.3&auto=format&fit=crop&w=1000&q=80"
}
```

#### Обновить Отель
```http
PUT /api/admin/hotels/{hotel_id}
```
**Описание**: Обновить информацию об отеле  
**Аутентификация**: Требуется (роль admin)

#### Удалить Отель
```http
DELETE /api/admin/hotels/{hotel_id}
```
**Описание**: Удалить отель  
**Аутентификация**: Требуется (роль admin)

#### Создать Номер
```http
POST /api/admin/rooms
```
**Описание**: Создать новый номер  
**Аутентификация**: Требуется (роль admin)

**Тело Запроса**:
```json
{
    "hotel_id": 1,
    "title": "Люкс",
    "description": "Описание номера",
    "type": "deluxe",
    "price": 150.00,
    "floor_area": 35,
    "image_url": "https://images.unsplash.com/photo-1631049307264-da0ec9d70304?ixlib=rb-4.0.3&auto=format&fit=crop&w=1000&q=80"
}
```

#### Обновить Номер
```http
PUT /api/admin/rooms/{room_id}
```
**Описание**: Обновить информацию о номере  
**Аутентификация**: Требуется (роль admin)

#### Удалить Номер
```http
DELETE /api/admin/rooms/{room_id}
```
**Описание**: Удалить номер  
**Аутентификация**: Требуется (роль admin)

#### Статистика Системы
```http
GET /api/admin/stats
```
**Описание**: Получить статистику системы  
**Аутентификация**: Требуется (роль admin)

### 🏨 API Менеджера

**⚠️ Требуется Роль Менеджера**: Все эндпоинты ниже требуют роль 'manager'

#### Как Стать Менеджером
1. Зарегистрируйте пользователя через API (шаги 1-2 выше)
2. Обновите роль пользователя в базе данных:
```sql
UPDATE users SET role = 'manager' WHERE email = 'manager@example.com';
```
3. Назначьте отель менеджеру:
```sql
UPDATE hotels SET manager_id = (SELECT id FROM users WHERE email = 'manager@example.com') WHERE id = 1;
```
4. Войдите снова для получения прав менеджера

#### 36. Получить Отели Менеджера
```http
GET /api/manager/hotels
```
**Аутентификация**: Требуется (роль manager)  
**Заголовки**: `Accept: application/json`

#### 37. Получить Бронирования Отеля (Менеджер)
```http
GET /api/manager/bookings
```
**Аутентификация**: Требуется (роль manager)  
**Заголовки**: `Accept: application/json`

**Опциональные Фильтры**:
- `?hotel_id=1` - Фильтр по конкретному отелю
- `?status=confirmed` - Фильтр по статусу бронирования
- `?date_from=2025-01-01` - Фильтр с даты
- `?date_to=2025-12-31` - Фильтр до даты

#### 38. Обновить Статус Бронирования
```http
PUT /api/manager/bookings/1
```
**Аутентификация**: Требуется (роль manager)  
**Заголовки**: `Accept: application/json`, `Content-Type: application/json`

**Тело Запроса**:
```json
{
    "status": "confirmed"
}
```

**Доступные Статусы**:
- `confirmed` - Бронирование подтверждено
- `cancelled` - Бронирование отменено  
- `pending` - Бронирование ожидает одобрения

#### Получить Бронирования Отеля (Менеджер)
```http
GET /api/manager/bookings
```
**Описание**: Получить бронирования для отелей менеджера  
**Аутентификация**: Требуется (роль manager)

**Параметры Запроса**:

| Параметр | Тип | Описание | Пример |
|-----------|------|-------------|----------|
| `hotel_id` | integer | Фильтр по конкретному ID отеля | `200` |
| `status` | string | Фильтр по статусу бронирования | `confirmed`, `cancelled`, `pending` |
| `date_from` | date | Фильтр бронирований с даты (YYYY-MM-DD) | `2025-01-01` |
| `date_to` | date | Фильтр бронирований до даты (YYYY-MM-DD) | `2025-12-31` |

**🧪 Полное Руководство по Тестированию**:

**Предварительные Требования**:
1. Пользователь должен иметь `role = 'manager'` в базе данных
2. Отель должен иметь `manager_id`, установленный на ID текущего пользователя
3. Бронирования должны существовать для номеров отеля

**Шаг 1: Настройка Пользователя-Менеджера**
```sql
-- Обновить роль пользователя на менеджера
UPDATE users SET role = 'manager' WHERE id = 1;

-- Назначить отель менеджеру
UPDATE hotels SET manager_id = 1 WHERE id = 200;
```

**Шаг 2: Создать Тестовое Бронирование**

**Важно**: Убедитесь, что `room_id` принадлежит отелю менеджера!

```http
POST /api/bookings
Content-Type: application/json
```
```json
{
    "room_id": 1,
    "started_at": "2025-12-01",
    "finished_at": "2025-12-05",
    "special_requests": "Тестовое бронирование для менеджера"
}
```

**⚠️ Критично**: `room_id` должен принадлежать отелю, где текущий пользователь является менеджером. Проверьте с помощью:
```sql
SELECT r.id as room_id, r.title as room_title, h.id as hotel_id, h.title as hotel_title, h.manager_id
FROM rooms r 
JOIN hotels h ON r.hotel_id = h.id 
WHERE h.manager_id = YOUR_USER_ID;
```

**Шаг 3: Тестирование API Бронирований Менеджера**

**Базовый Запрос**:
```http
GET /api/manager/bookings
```

**Фильтр по Отелю**:
```http
GET /api/manager/bookings?hotel_id=200
```

**Общие Проблемы**:
- **Пустые данные**: Пользователь не назначен менеджером отеля
- **403 Unauthorized**: Роль пользователя не 'manager'
- **Нет бронирований**: Не существует бронирований для отелей менеджера

**🔍 Устранение Неполадок Пустого Ответа**:

Если вы получаете пустые данные `{"success": true, "data": []}`, проверьте:

**Шаг 1: Проверить Настройку Менеджера**
```sql
-- Проверить ID и роль текущего пользователя
SELECT id, full_name, email, role FROM users WHERE id = YOUR_USER_ID;

-- Проверить, назначен ли отель менеджеру
SELECT id, title, manager_id FROM hotels WHERE manager_id = YOUR_USER_ID;

-- Проверить, принадлежит ли номер отелю менеджера
SELECT r.id, r.title, r.hotel_id, h.title as hotel_title, h.manager_id 
FROM rooms r 
JOIN hotels h ON r.hotel_id = h.id 
WHERE h.manager_id = YOUR_USER_ID;
```

**Шаг 2: Проверить Существование Бронирования**
```sql
-- Проверить, существует ли бронирование для номеров отеля менеджера
SELECT b.id, b.room_id, b.user_id, b.status, r.hotel_id, h.manager_id
FROM bookings b
JOIN rooms r ON b.room_id = r.id
JOIN hotels h ON r.hotel_id = h.id
WHERE h.manager_id = YOUR_USER_ID;
```

#### Обновить Статус Бронирования
```http
PUT /api/manager/bookings/{booking_id}
```
**Описание**: Обновить статус бронирования  
**Аутентификация**: Требуется (роль manager)

**Тело Запроса**:
```json
{
    "status": "confirmed"
}
```

**Доступные Значения Статуса**:
- `"confirmed"` - Бронирование подтверждено
- `"cancelled"` - Бронирование отменено
- `"pending"` - Бронирование ожидает одобрения

### 🔧 Утилиты API

#### Обновить Часовой Пояс Пользователя
```http
POST /api/update-timezone
```
**Описание**: Обновить часовой пояс аутентифицированного пользователя  
**Аутентификация**: Требуется (на основе сессии)  
**Content-Type**: `application/json`

**Тело Запроса**:
```json
{
    "timezone": "Europe/Moscow"
}
```

#### Получить Текущего Пользователя
```http
GET /api/user
```
**Описание**: Получить текущего аутентифицированного пользователя  
**Аутентификация**: Требуется (токен Sanctum)

### 📊 Формат Ответа API

Все API эндпоинты следуют единому формату ответа:

**Успешный Ответ**:
```json
{
    "success": true,
    "data": {},
    "message": "Опциональное сообщение об успехе"
}
```

**Ответ с Ошибкой**:
```json
{
    "success": false,
    "message": "Сообщение об ошибке",
    "errors": {
        "field_name": ["Описание ошибки"]
    }
}
```

**HTTP Коды Статуса**:
- `200` - Успех
- `201` - Создано
- `401` - Неавторизован
- `403` - Запрещено
- `404` - Не найдено
- `422` - Ошибка валидации
- `500` - Ошибка сервера

## 🧪 Тестирование с Postman/Insomnia

### Требования для Настройки
1. **Базовый URL**: `http://localhost:8000`
2. **Заголовки**: Всегда включайте `Accept: application/json`
3. **Сервер Laravel**: `php artisan serve`
4. **База Данных**: `php artisan app:quick-setup`

### Полная Последовательность Тестирования

**Основной Пользовательский Поток**:
1. Регистрация без 2FA (шаг 1)
2. Вход (шаг 2)
3. Просмотр отелей (шаг 8)
4. Создание бронирования (шаг 23)
5. Просмотр бронирований (шаг 24)
6. Выход

**Поток Пользователя с 2FA**:
1. Регистрация с 2FA (шаг 3)
2. Выбор метода 2FA (шаг 4A или 4B)
3. Проверка 2FA (шаг 5 или 6)
4. Вход с 2FA (шаг 7)
5. Просмотр и бронирование отелей

**Поток Админа**:
1. Регистрация пользователя и повышение до админа (шаг 27)
2. Управление отелями (шаги 29-31)
3. Управление номерами (шаги 32-34)
4. Просмотр статистики (шаг 35)

**Поток Менеджера**:
1. Регистрация пользователя и повышение до менеджера (шаг 36)
2. Назначение отеля менеджеру
3. Просмотр бронирований отеля (шаг 37)
4. Обновление статуса бронирования (шаг 38)

### 🔍 Устранение Неполадок

**Общие Проблемы**:
- **403 Unauthorized**: Проверьте роль пользователя в базе данных
- **Пустые ответы**: Проверьте, что данные существуют и пользователь имеет доступ
- **HTML ответы**: Добавьте заголовок `Accept: application/json`
- **2FA коды**: Проверьте `storage/logs/laravel.log` для email кодов

**Запросы Базы Данных для Настройки**:
```sql
-- Сделать пользователя админом
UPDATE users SET role = 'admin' WHERE email = 'admin@example.com';

-- Сделать пользователя менеджером и назначить отель
UPDATE users SET role = 'manager' WHERE email = 'manager@example.com';
UPDATE hotels SET manager_id = (SELECT id FROM users WHERE email = 'manager@example.com') WHERE id = 1;

-- Проверить роли пользователей
SELECT id, full_name, email, role FROM users;

-- Проверить назначения отелей
SELECT h.id, h.title, h.manager_id, u.full_name as manager_name 
FROM hotels h 
LEFT JOIN users u ON h.manager_id = u.id;
```

### Отмена Бронирования - Важные Примечания:

**Как работает отмена**:
- Бронирования НЕ удаляются из базы данных
- Статус меняется на `"cancelled"`
- Отмененные бронирования по-прежнему отображаются в `GET /api/bookings`
- Это сохраняет историю бронирований для аудита

### Как получить booking_id для отмены:
1. После создания бронирования запомните поле `id` из ответа
2. Или вызовите `GET /api/bookings` чтобы увидеть все ваши бронирования с их ID
3. Используйте значение `id` в URL DELETE: `/api/bookings/{id}`

**Пример рабочего процесса**:
```http
# Шаг 1: Создать бронирование
POST /api/bookings
# Ответ: {"data": {"id": 1, "status": "confirmed", ...}}

# Шаг 2: Отменить бронирование
DELETE /api/bookings/1

# Шаг 3: Проверить бронирования (отмененное по-прежнему отображается)
GET /api/bookings
# Ответ: {"data": [{"id": 1, "status": "cancelled", ...}]}
```

## 📚 Дополнительные Ресурсы

- **Веб-Маршруты**: См. исходный код для полных веб-маршрутов (браузер)
- **Схема Базы Данных**: Запустите миграции, чтобы увидеть полную структуру базы данных
- **Примеры Кода**: Проверьте файлы контроллеров для деталей реализации
- **Тестирование**: Запустите `php artisan test` для комплексного набора тестов

## 🐛 Устранение Неполадок

### API Возвращает HTML Вместо JSON
**Проблема**: Получение HTML ответа от `/hotels` вместо JSON  
**Решение**: Используйте эндпоинт `/api/hotels` для JSON ответа

### Ошибка 419 (CSRF Token Mismatch)
**Проблема**: CSRF ошибка при тестировании API  
**Решение**: 
- Используйте маршруты `/api/*` (исключены из CSRF)
- Убедитесь, что `APP_ENV=local` в `.env`
- Очистите кеш: `php artisan config:clear`

### Ошибка 404 на API Маршрутах
**Проблема**: API маршруты не найдены  
**Решение**:
- Проверьте, что `api.php` зарегистрирован в `bootstrap/app.php`
- Очистите кеш маршрутов: `php artisan route:clear`
- Проверьте маршруты: `php artisan route:list --path=api`

### Ошибка 500 "Auth guard [sanctum] is not defined"
**Проблема**: Аутентификация Sanctum не настроена для эндпоинта `/api/user`  
**Решение**: 
- Это ожидаемо - Sanctum не настроен в этой среде разработки
- Используйте эндпоинты аутентификации на основе сессий
- Для продакшена установите и настройте Laravel Sanctum

### Ошибка 422 (Validation Error) при Проверке Доступности Номера
**Проблема**: Ошибка "The room id field is required"  
**Решение**: 
- Используйте заголовок `Content-Type: application/json`
- Выберите "raw" и "JSON" в теле Postman
- Убедитесь, что все обязательные поля включены: `room_id`, `started_at`, `finished_at`
- Используйте правильный формат даты: `YYYY-MM-DD`
- Используйте будущие даты (не прошедшие)

### Postman Показывает "Loading..." Бесконечно
**Проблема**: Вкладка Preview показывает бесконечную загрузку  
**Решение**: Используйте вкладку "Body" или "Pretty" вместо "Preview" для JSON ответов

## 🔒 Уведомление о Безопасности API

**Только для Разработки**: Маршруты аутентификации API (`/api/register`, `/api/login`, `/api/logout`) доступны **ТОЛЬКО в локальной среде разработки** (`APP_ENV=local`).

**Для Продакшена**: 
- Эти маршруты автоматически отключаются в продакшене
- Используйте Laravel Sanctum для правильной API аутентификации
- Никогда не отключайте CSRF защиту в продакшене
- Всегда используйте правильные токены аутентификации для доступа к API

**Проверьте ваш .env файл**:
```env
APP_ENV=local  # API тестовые маршруты включены
# APP_ENV=production  # API тестовые маршруты отключены
```

---

**Создано с ❤️ используя Laravel 12 для образовательных целей**